# V2.4 M1 — Integration Proofs

Estado: **P-01 y P-02 PASS (automatizadas con evidencia de imagen,
2026-09-02)**. P-03 permanece manual (requiere visor GraphML de
escritorio instalado).

M1 gates (docs/v2/59):
*LikeC4 y draw.io con integration proof real* y *GraphML probado en un
viewer externo*. Todo lo automatizable ya está verificado por `mise run
ci` (python 404, bats 75); estas pruebas cierran el milestone.

Material de prueba: artefactos reales del run Next.js
(`artifacts/oss/nextjs-20260902/`, manifest sha256 verificado, 87
elementos + 55 relaciones, fuente `vercel/next.js@ec847d82`).

---

## P-01 · LikeC4 managed server — **PASS (automatizada + visual)**

Ejecución: `python/.venv/bin/python scripts/uat/m1-proof/verify-likec4-server.py
--workspace artifacts/oss/nextjs-20260902/project`

Resultados verificados:
- launch del viewer `likec4-server` (MANAGED_SERVER) resuelve el CLI
  completo de mise **por capacidad** (probe `--help` busca `start`: el
  shim asdf del PATH es export-only y se descarta — hallazgo real);
- `likec4 start` sirve el workspace real: HTTP 200 text/html en
  `:5173`;
- lease registrada en RuntimeRegistry (`viewer-likec4-server`) mientras
  corre;
- `stop()` mata el proceso y limpia la registry (post-teardown:
  `pid_alive: false`, registry vacía).

Evidencia visual (`artifacts/uat/m1/evidence/`):
- `likec4-server-ui.png` (sha256 `942b9808c45ad1e7…`): la app LikeC4
  real sirviendo nuestro modelo — vista `index` con el elemento raíz
  `repo` (SYSTEM), inspector funcional (Properties/Relationships/
  Views/Structure/Deployments).
- `likec4-structure.png` (sha256 `ebb5c018c96b62f7…`): inspector del
  elemento.

**Hallazgo registrado (follow-up, no bloquea el gate)**: la vista
generada `view index { include * }` muestra un único nodo colapsado
porque el adapter anida los 87 elementos bajo un solo sistema raíz;
LikeC4 solo muestra el scope raíz. Mejora pendiente del LikeC4
projection adapter: vistas con include acotado (`include repo.*`) o
una vista por elemento. El contenido completo viaja en las demás
proyecciones (draw.io renderizado abajo, GraphML, Canvas) y el modelo
se parsea e inspeciona correctamente.

- [x] PASS

---

## P-02 · draw.io embed mode — **PASS (automatizada con imágenes)**

Ejecución: `python3 scripts/uat/m1-proof/generate-drawio-embed.py … &&
node scripts/uat/m1-proof/verify-drawio-embed.mjs` (Playwright 1.62.1,
chromium headless).

Protocolo verificado contra embed.diagrams.net real:
1. handshake `← init` recibido (protocolo actual; `load` legacy
   soportado);
2. `→ load` del XML real de Next.js aceptado por el editor;
3. `→ export` (format xmlpng) devuelto: **521.273 bytes** de PNG
   (umbral anti-lienzo-vacío: 20 KB);
4. round-trip base operativa (el export es la entrada del
   `ProjectionDelta → proposal` de M5, docs/v2/54 §8).

Validación visual de las imágenes (análisis del render):
- `02-loaded.png` (sha256 `5e9bf8b556d90a9b…`): ~80–110 formas
  (87 elementos esperados), layout jerárquico con clústeres azules y
  nodos interfaz naranjas, etiquetas legibles.
- `exported-architecture.png` (sha256 `c7d4cc30adc95b2c…`, 521 KB):
  ~100 formas con etiquetas reales de Next.js
  (`app/api/posts/route.ts`, `app/api/preview/route.ts`,
  `secret-key`, `DELETE`/`GET`/`POST`, `interface · DETECTED/high`).

- [x] PASS

---

## P-03 · GraphML en viewer externo — **PENDIENTE (manual)**

Artefacto: `artifacts/oss/nextjs-20260902/project/graphml.graphml`
(23.9 KB). Ningún visor GraphML está instalado en este host (decisión
documentada: no convertirlos en dependencias).

Opciones (elige una):
1. **yEd** (desktop, gratuito): File → Open → `graphml.graphml`
2. **Cytoscape Desktop**: File → Import → Network from File
3. **Gephi**: File → Open (acepta GraphML directamente)

**Esperado**: grafo importable con 87 nodos / 55 aristas; atributos
`kind`/`origin` como data keys.

**Evidencia**: captura del visor con el grafo cargado + versión del
visor usada.

- [ ] PASS / [ ] FAIL — notas:

---

## Cierre del gate

Con P-01 y P-02 automatizados y validados visualmente, y P-03 como
única verificación manual restante, el gate M1 queda en 7/8 criteria.
Al cerrar P-03: marcar su check en `docs/v2/59-v2.4-milestones.md` (M1)
y actualizar la fila V2.4 de `docs/v2/STATUS.md`.
