# V2.4 Milestones and Acceptance Gates

## M0 Product Kernel

Implemented 2026-09-02 (commits `b4b6bbd`…`37f49e6`; evidence: STATUS V2.4 row, tests in `python/tests/test_application_*.py`, `test_runtime_state.py`, `test_cli_v24.py`; `mise run ci` green).

### Acceptance

- [x] ArchitectureSnapshot schema versionado y digest estable.
- [x] Snapshot contiene git/code/world/policy revisions.
- [x] RunLedger persiste run summaries sin mezclar logs en ArchitectureWorld.
- [x] RuntimeRegistry sobre XDG runtime y orphan cleanup probado.
- [x] `ArchitectureQueryPort` y `GovernancePort` consumibles sin `world.graph` fuera de infra/domain boundary.
- [x] CLI legacy tests no regresan.
- [x] STATUS/ADR index sin numeración V2.2 duplicada.

### Exit trigger negativo

Si implementar Snapshot exige duplicar el world completo en DTOs enormes, parar y diseñar snapshot manifest + lazy refs.

## M1 Daily Driver / Viewers

In progress 2026-09-02: 7/8 acceptance verificados (commits `c892e9b`, `58e2494`, `1e42b43`, `5cf4b12`; proofs automatizadas en `docs/v2/uat/v2.4-m1-integration-proofs.md`). Follow-up abierto: LikeC4 adapter — vistas con include acotado (la vista por defecto colapsa el modelo en el sistema raíz).

### Acceptance

- [x] `status --json` estable.
- [x] actions tipadas y command rendering separado.
- [x] `schema status` válido.
- [x] `explain` soporta element y relation.
- [x] ViewerRegistry descubre availability.
- [x] LikeC4 y draw.io con integration proof real.
- [ ] GraphML probado en viewer externo.
- [x] artifact generation independiente de viewer availability.

### Exit trigger negativo

Si ViewerAdapter empieza a contener transformación de dominio, mover esa lógica a ProjectionAdapter antes de seguir.

## M2 Change Intelligence

Implemented 2026-09-02 (commits `ff4d0fa`…`f0d90bd`; evidence: STATUS V2.4 row, tests in `python/tests/test_context_v24.py`, `test_impact_gaps.py`, `test_ask_sessions.py`, `test_prompt_compiler.py`, `test_source_read_policy_uat.py`, `test_bootstrap.py`; `mise run ci` green).

### Acceptance

- [x] `ContextQuery` explícito.
- [x] `ask` NL y typed producen intención equivalente en fixtures.
- [x] `impact` sobre file/symbol/element.
- [x] KnowledgeGap persisted/auditable.
- [x] AgentSession stale detection.
- [x] Context KPIs bytes/lines/tokens/evidence density.
- [x] source-read policy UAT.

## M3 Governance

- [x] history de 3+ real runs.
- [x] ArchitectureDelta golden tests.
- [x] Fitness Profile deterministic.
- [x] gate invariants + exit codes.
- [x] Waiver expiry.
- [x] SARIF validator PASS.
- [x] PR report base/head.
- [x] signed snapshot spike (documented in [65-signed-snapshot-spike.md](65-signed-snapshot-spike.md); CI integration + rotación quedan como follow-up).

## M4 Agent Distribution

- [x] MCP read tools E2E.
- [x] propose tools E2E.
- [x] admin disabled by default.
- [x] prompt hash/skill revision recorded for embedded LLM candidate.
- [x] candidate -> review -> promote path.
- [x] simulate never mutates base world.
- [x] fixture replay deterministic.

## M5 Control Plane

Slice 20 implemented 2026-09-03 (evidence: `python/src/archskillkit/delivery/cli/control_plane.py`, tests in `python/tests/test_control_plane.py`; `mise run ci` green). Local-only HTTP kernel: binds 127.0.0.1 by construction (no escape hatch), per-process bearer token printed once on stdout (single-line JSON envelope, never persisted), RuntimeRegistry register/unregister on graceful shutdown, and a read-only JSON API over the application layer (`/health`, `/status`, `/history`, `/viewers` — same schema-bound envelopes as their CLI counterparts).

Slice 21 implemented 2026-09-03 (evidence: same `control_plane.py` + new tests, `mise run ci` green). New schema-bound endpoints: `/evidence` (evidence + provenance), `/coverage` (coverage/unknowns counts), `/gaps` (open knowledge gaps), `/findings` (governance findings). All reuse existing application queries; no new domain logic. Static zero-dependency Control Plane shell served at `/` without arbitrary filesystem access, keyboard-operable, semantic HTML, WCAG 2.2 AA contrast, `prefers-reduced-motion` safe. Trust trade-off documented in module docstring.

Slice 22 implemented 2026-09-03 (evidence: `control_plane.py` + 23 new tests, `mise run ci` green). Viewer Hub: projection format selector and viewer selector in the static shell. `/projections` returns available formats with `artifact_status` (never `artifact_path`). `/launch` POST accepts `{"format", "viewer"}` (strict schema, extra keys → 400), resolves artifact server-side from `ARTIFACT_PATHS` inside `world.workspace` (browser never supplies paths), rejects missing artifact before routing (matching `delivery/cli/view.py` order), and reuses `ViewerRegistry.route` + `launch()` for the operational side effect. Bearer token required, CSP unchanged, no governance writes. LikeC4/draw.io/system-default are swappable via the viewer selector.

Slice 23 (draw.io semantic edit → proposal candidate) is IN PROGRESS behind a blocking experiment. **23a PASSED 2026-09-03** (evidence: `scripts/uat/m5-slice-23a/README.md`, fixtures in `artifacts/uat/v2.4/m5-slice-23a/`, test `python/tests/test_drawio_delta.py`; `mise run ci` green): real embed.diagrams.net round trip proved that `export format=json` drops semantic cells (REJECTED, negative fixture preserved), `/`-containing attribute names are invalid XML (replaced by `archskillkit-*` kebab-case keys), `load` strips UserObject vertex metadata, and the `merge` channel (`load(blank)` → `merge(artifact-as-mxGraphModel)`, full-state composition for edits) preserves `UserObject`+custom attributes through deterministic exports (canonical sha `34a7d186…`, RUN1==RUN2). **23b implemented 2026-09-03** (evidence: `python/src/archskillkit/projections/drawio_delta.py`, 25 tests in `test_drawio_delta.py` incl. the real captured fixture; mypy scope extended; `mise run ci` green): pure `classify_xml(base_artifact, submitted_xml, submitted_origin)` classifier — element/relation added-removed-kind_changed as semantic candidates, renames surface as remove+add (never aliased), style/geometry-only diffs and metadata-less edges as presentation changes, and stable unsupported reason codes (`NO_VERTEX_NAME`, `UNKNOWN_ELEMENT_KIND`, `INCOMPLETE_RELATION`, `UNRESOLVED_RELATION_ENDPOINT`, `DUPLICATE_IDENTITY`, `AMBIGUOUS_CELL`, `MALFORMED_XML`, `NON_EMBED_ORIGIN`) for the delivery layer to refuse forking. The HTTP route (23c) and shell wiring (23d) are NOT started; the M5 checkbox below flips only when all four sub-slices land. **23c implemented 2026-09-03** (evidence: `POST /drawio-candidate` in `control_plane.py`, 11 E2E tests in `test_control_plane.py`; `mise run ci` green): strict body schema (`name` matching `^[A-Za-z0-9_-]{1,64}$`, `format == "drawio"`, `export` XML string, 4 MiB cap, extra keys → 400), server-side base artifact resolution (never browser paths), `409 ARTIFACT_MISSING` / `409 BASE_DRIFT` gates against the projection metadata sidecar, classification via 23b (`422 UNSUPPORTED_EDITS` with the unsupported list and NO fork when ambiguous), presentation-only submissions return `candidate: null` without forking, and semantic submissions apply the candidates on a fresh `proposal-<name>` fork (latest submission wins: prior same-name fork is dropped), record the proposal object and return the structural diff. Approve/reject/promote remain unreachable from this endpoint. **23d implemented 2026-09-03** (evidence: shell wiring + `GET /drawio-artifact` in `control_plane.py`, 7 tests in `test_control_plane.py`; `mise run ci` green): the Viewer Hub gains an "Edit draw.io" action (drawio format + artifact present only) that opens the embed editor in an iframe `sandbox="allow-scripts"` with `referrerpolicy="no-referrer"`, initializes it through the metadata-preserving channel (`load(blank)` → `merge(artifact)` — slice-23a rules), and posts the user's export to `/drawio-candidate` from an exact-origin message pump (`https://embed.diagrams.net` in both directions, never `*`). CSP gains exactly one directive: `frame-src https://embed.diagrams.net`. The shell surfaces classification counts, unsupported cells, the candidate run id and a permanently disabled Promote control (governance opt-in is slice 24). New auth-gated `GET /drawio-artifact` serves the projection XML path-free with a `base_drift` flag (enforcement stays in the candidate endpoint). Slice 23 CLOSED.

- [x] local-only server default.
- [x] evidence panel.
- [x] unknown/coverage panel.
- [x] viewer selector.
- [x] draw.io semantic edit -> proposal candidate.
- [x] LikeC4/other viewer swappable.
- [ ] governance mutations opt-in.

## M6 Learning

- [ ] SensorCandidate schema.
- [ ] generated positive/negative fixture set reviewed.
- [ ] deterministic sensor precision/recall threshold met.
- [ ] accepted sensor revision linked to originating discoveries.
- [ ] Conformance rule candidate requires human/policy approval.
